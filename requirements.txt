# MissionGuard phase 1 — pin versions used with Python 3.14 (Windows).
# Install only inside .venv:  python -m pip install -r requirements.txt
pandas==3.0.5
numpy==2.5.3
scikit-learn==1.9.1
joblib==1.6.0
pytest==9.1.1
